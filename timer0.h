// ==========================================================================
// 타이머0 분주 및 1ms 인터럽트 제어 헤더 (timer0.h)
// ==========================================================================
#ifndef TIMER0_H_
#define TIMER0_H_

#include <avr/io.h>

// 메인 및 타 레이어에서 참조할 절대 기준 ms 카운터 전역 변수
extern volatile uint32_t ms_count;

// 타이머0 하드웨어 초기화 함수 프로토타입
void timer0_init(void);

#endif // TIMER0_H_